/*const userSchema = new mongoose.Schema({
    id: Number,
    private: Number, //0 = others can view profile, 1 = only friends, 2 = no one can view
    featuredGames: Array[Number(game ids)],
    games: Array[Number(game ids)],
    reviews: Array[Number(review ids)],
    youtube: String,
    twitch: String,
    other socials...,
    friends: Array[Number(user ids)]
})
*/